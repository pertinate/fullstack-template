{
  "name": "server",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index",
    "test": "tap --ts",
    "dev": "ts-node-dev --respawn --transpile-only --exit-child --require dotenv/config src/index.ts"
  },
  "dependencies": {
    "@fastify/static": "^6.10.2",
    "@prisma/client": "^4.16.0",
    "dotenv": "^16.3.1",
    "fastify": "^4.18.0",
    "typescript": "^5.1.3"
  },
  "devDependencies": {
    "@types/tap": "^15.0.8",
    "mock_payloads": "*",
    "tap": "^16.3.6",
    "ts-node-dev": "^2.0.0",
    "types": "*"
  }
}
